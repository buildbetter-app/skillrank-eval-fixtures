# Integration directory brief

Build a small static integration directory from `integrations.csv` for RelayOps, a workflow automation product. The pages should help a reader decide whether an integration fits their workflow rather than merely swapping keywords into the same paragraph.

The canonical production origin is `https://relayops.example`.

Do not invent ratings, customer counts, testimonials, pricing, or capabilities beyond the supplied data.
