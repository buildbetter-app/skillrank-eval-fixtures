# duration fixture

Implement `parse_duration` in `src/duration.py` per its docstring specification.
Do not add or rely on any test files — your implementation will be checked
against a hidden test suite. Match the spec exactly, including the error cases.
